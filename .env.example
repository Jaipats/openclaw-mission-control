# OpenClaw Gateway Configuration

# OpenClaw Gateway URL (default OpenClaw Control UI port: 18789)
# If you have OpenClaw installed, it typically runs at http://127.0.0.1:18789
# Mission Control will sync with OpenClaw's ~/.openclaw/openclaw.json configuration
OPENCLAW_GATEWAY_URL=http://localhost:18789

# Mission Control Server Port
SERVER_PORT=4000

# WebSocket Update Interval (ms)
WS_UPDATE_INTERVAL=100

# Configuration Sync Interval (ms) - how often to check for OpenClaw config changes
CONFIG_SYNC_INTERVAL=10000
